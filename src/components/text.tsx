import styled from 'styled-components';
import { pastelPalette } from '../constants/colors';

export const Text = styled.p`
  font-size: 1em;
  color: ${pastelPalette.purple};
  margin: 0;
`;

export const InstructionText = styled.p`
  font-size: 2em;
  color: ${pastelPalette.purple};
  margin: 0;
`;
